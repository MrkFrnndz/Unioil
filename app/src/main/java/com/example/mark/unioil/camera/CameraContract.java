package com.example.mark.unioil.camera;

/**
 * Defines the contract between the CameraView {@link CameraActivity}
 * and CameraPresenter {@link CameraPresenter}.
 */

interface CameraContract {
    interface CameraView {

    }

    interface CameraPresenter {

    }
}
